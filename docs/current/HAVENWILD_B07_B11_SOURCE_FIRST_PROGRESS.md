# Havenwild B07–B11 source-first milestone: source-verified progress (September 17, 2026)

## Implemented in this incremental handoff (requires B06)

- B07 (partial): machine-readable, read-only source-binding audit distinguishes the mandatory W3 source-derived cliff cache, the *quarantined optional* OGA grass-top cliff/diagonal-ramp family, and separate LPC Revised building stairs. Missing OGA does not masquerade as a missing mandatory cliff face. Exact original hash and source URL remain in the existing authorities. `python tools/automation/assets/Audit-SourceFirstVisualBindingsB07.py --out artifacts/audits/b07_source_status.json` produces a local report without artwork mutation.
- B08 (partial): the native editor and client do not attempt to decode a non-existent quarantined OGA PNG. They still load the real W3 ElizaWy cliff faces. They do not substitute stair artwork for ramps. If the original OGA source is intentionally restored, its SHA-256 must match the source contract before enabling it as a published visual family. No connector visual certification is claimed.
- B09 (partial): editor/Pixel Studio cliff face projection no longer treats an out-of-bounds receiver as an established lower-level cell. Unknown neighboring partitions remain unresolved, not automatically rendered as an invented drop. Full global neighbor-aware transition resolution is outstanding.
- B10 (partial): uniform 3×3 pure-water interiors bypass costly medium-rim lookups for owner fills; uniform 4×4 pure-water halos skip all mixed-tuple work. Near-shore, depth contacts, and partition borders take the existing exact source resolver. This is not a retained renderer and does not establish FPS improvement.
- B11 (preparation): source role checks, water halo tests and an acceptance checklist are added below. The Windows gate, live FPS, editor/client visual parity and traversal acceptance remain outstanding.

## Critical source identity correction

The source `LPC_cliffs_grass.png` is from OpenGameArt's **LPC cliffs/mountains with grass top and more!** and includes authored cliff transitions and diagonal ramps. It is **not** the building/porch stairs family. Actual short building stairs are in `assets/source/licensed/lpc_revised/Structure/Stairs/Short Steps A.png`. Never use either to replace the other. The OGA source pack is explicitly `production_enabled=false`; its absence is an optional authoring-source gap, not a reason to declare real W3 cliff walls missing.

## Acceptance still needed for B11

1. Mandatory W3 cliff overlay regenerates/loads with B05C's pinned source and contract.
2. OGA remains optional, or its real original PNG has the exact catalog hash, review approval and license credits. Do not use a porch-stair image as a substitute.
3. Compare authored grass/dirt/rock plateau assignments to the source sheets. Any inappropriate rocky plateau is a material assignment issue, not proof that rock pixels are fabricated.
4. Verify front/rear/cliff corners, ramps, ladders and collisions in editor **and** client. Unknown/missing mapping is displayed as a diagnostic; no invented replacement.
5. Test deep ocean, depth rim, shore and chunk-edge water at identical coordinates; all seam defects require known global-neighbor resolution, not random source tiles.
6. Capture frame times, draw counts and frame stability at water-heavy zoom/pan before declaring a water performance pass. This patch removes redundant lookups but **does not** implement a retained partition renderer.
7. Once B09/B10 are truly complete and the above tests pass, stamp B11 visual certification. Do not call this progress patch a completed B11.

## Handoff

Drop the unextracted incremental ZIP into root for existing PCC intake, then use the usual Full Gate. No separate patch script or new framework. Preserve older scenes, existing authored Base World, and original source assets.
