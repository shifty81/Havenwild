# Pass 148Q — State Geometry and Behavior Execution

Placeable states may now override collision, interaction, and movement-blocking footprints. Open doors become nonblocking, harvested trees use stump geometry, and occupied furniture exposes state-scoped attachment points. Stable behavior-node IDs compile into deterministic runtime commands and execute only after the authority gate accepts the state transition.
